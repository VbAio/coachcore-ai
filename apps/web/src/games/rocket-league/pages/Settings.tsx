import { ComingSoonPage } from '@/shared/components/coming-soon-page';
import type { GamePageProps } from '@/games/types';

export function RocketLeagueSettingsPage(_p: GamePageProps) {
  return <ComingSoonPage feature="Settings" />;
}
